# Установка базового образа
FROM node:16

RUN mkdir -p app

# Установка рабочей директории в контейнере
WORKDIR /app

# Копирование остальных файлов проекта
COPY . .

# Открытие порта 3000 для доступа к серверу извне
EXPOSE 3000

# Команда для запуска приложения
CMD ["node", "index.js"]
